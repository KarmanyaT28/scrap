docker run --rm -it \
  --platform linux/amd64 \
  --privileged \
  --network net-10.9.0.0 \
  --ip 10.9.0.53 \
  seed-local-dns-server bash



ip route
ip route del default
echo $?
ip route add default via 10.9.0.11
echo $?
/usr/sbin/named -g -c /etc/bind/named.conf
